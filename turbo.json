{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "!**/*.md",
        "src/**",
        "app/**",
        "public/**",
        "Cargo.toml",
        "Cargo.lock"
      ],
      "outputs": [".next/**", "!.next/cache/**", "pkg/**"],
      "env": ["NODE_ENV"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "src/**", "app/**"],
      "outputs": []
    },
    "//#version": {
      "cache": true
    },
    "packaging": {
      "dependsOn": ["build"],
      "cache": false
    },
    "publish": {
      "dependsOn": ["packaging"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": true,
      "persistent": true
    }
  }
}
