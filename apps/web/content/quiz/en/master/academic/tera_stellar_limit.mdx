---
id: tera_stellar_limit
difficulty: master
category: academic
format: choices
question: "Does the Stellar Tera Type's effect of 'boosting all move types' persist if you use moves of the same type more than once?"
options:
  - "Only once per type (subsequent moves of a used type receive the normal multiplier)"
  - "Always boosts all types until the Stellar Tera Type is deactivated"
  - "The boost lasts until the same type is used 3 times"
  - "Boosts are applied in order from lowest Speed"
correctAnswer: "Only once per type (subsequent moves of a used type receive the normal multiplier)"
---

The Stellar Tera Type grants a Same Type Attack Bonus (STAB) equivalent to a +20 base power boost for all types, but only once per type. Once a move of a certain type is used, subsequent uses of that type will receive the normal multiplier. The boosts remain until all types have been used.
